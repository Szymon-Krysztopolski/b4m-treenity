import React from "react";

export default function RegistrationForm() {

    return (
        <form className={"registration--form"} onSubmit={}>
            <input type="submit" value="RegistrationForm"/>
        </form>
    )
}