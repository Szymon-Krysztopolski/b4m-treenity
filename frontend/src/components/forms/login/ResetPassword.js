import React from "react";

export default function ResetPassword() {

    return (
        <form className={"resetPassword--form"} onSubmit={}>
            <input type="submit" value="Reset password"/>
        </form>
    )
}